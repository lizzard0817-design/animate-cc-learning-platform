[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["cd server && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building Python backend'"]

[start]
cmd = "cd server && uvicorn main:app --host 0.0.0.0 --port $PORT"
